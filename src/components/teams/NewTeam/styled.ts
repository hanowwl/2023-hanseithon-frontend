import styled from "@emotion/styled";

export const TeamElement = styled.div`
  display: inline;
  width: 37rem;
  margin-top: 1.6rem;
`;
export const TeamInformation = styled.div`
  padding: 2rem 2.4rem;
  border-radius: 1.2rem;
  background-color: #171717;
`;
export const TeamType = styled.div`
  width: 5.3rem;
  height: 2.2rem;

  border-radius: 0.5rem;
  background-color: #1774fe;
`;

export const TeamTypeText = styled.p`
  padding: 0.4rem 0.6rem;
  font-size: 1.2rem;
  font-weight: 500;
  letter-spacing: -0.048rem;
`;

export const TeamName = styled.h1`
  font-size: 1.8rem;
  font-weight: 700;
  letter-spacing: -0.072rem;

  margin-top: 0.6rem;
`;
export const TeamDescription = styled.p`
  color: #878b92;
  font-size: 1.6rem;
  font-weight: 400;
  letter-spacing: -0.064rem;

  margin-top: 0.4rem;
`;

export const TeamPositionContainer = styled.div`
  margin-top: 2rem;
`;
export const TeamLeader = styled.div`
  display: flex;
`;
export const TeamMembers = styled.div`
  display: flex;
  margin-top: 0.6rem;
`;
export const TeamPosition = styled.h2`
  color: #428eff;
  font-size: 1.6rem;
  font-weight: 700;
  letter-spacing: -0.064rem;

  margin-right: 0.6rem;
`;
export const TeamMember = styled.p`
  font-size: 1.6rem;
  font-weight: 500;
  letter-spacing: -0.064rem;
`;
