import styled from "@emotion/styled";

export const FileUploadContainer = styled.section`
  display: flex;
`;

export const FileList = styled.li`
  width: 31.2rem;

  margin-bottom: 1.1rem;
`;

export const FileDetailContainer = styled.div`
  display: inline-block;
`;

export const FileNameAndSize = styled.div`
  display: inline-flex;

  margin-bottom: 0.4rem;
`;

export const FileName = styled.h2`
  font-size: 1.6rem;
  font-weight: 700;
  letter-spacing: -0.064rem;
`;
export const FileSize = styled.h2`
  color: #5398ff;

  font-size: 1.6rem;
  font-weight: 700;
  letter-spacing: -0.064rem;

  margin-left: 0.4rem;
`;

export const FilePathname = styled.p`
  color: #878b92;

  font-size: 1.4rem;
  font-weight: 400;
  letter-spacing: -0.056rem;
`;

export const UploadInputContainer = styled.div`
  position: relative;

  margin: auto 0 auto auto;
`;
